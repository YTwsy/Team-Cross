# 2026-09-23 协作详情阅读布局反馈验证

## 环境与范围

- 以 `469f570` 为基线，在 `codex/feature/reading-page-scroll` 工作区完成修改；本记录对应本次增量源码与重新构建的内嵌 Web 资源，不代表已在用户的“应用程序”目录安装 App。
- macOS 14.8.5 / arm64，Go 1.27.1，Node 24.18.0，pnpm 11.19.0。
- 调整执行详情的上下文类型栏、对话正文开头的间距、概览位置切换上下文类型时的整页滚动、专注阅读工具栏的垂直对齐，以及详情页顶部和批注卡片的排版。阅读中的段落位置、批注定位和材料读取沿用既有规则。

## 修改与工程检查

- 上下文类型标签收紧横向间距，并与目录、专注入口使用同一行高；窄阅读栏不再裁掉“技术信息”。目录按钮容器改为 flex，消除行框额外高度造成的图标上移。
- 消除上下文面板、阅读网格、历史正文和轮次标题叠加的顶部留白。吸顶栏下缘至第一轮标题为 8 CSS px。
- 在阅读栏尚未吸顶的概览位置切换上下文类型时，以当前整页滚动坐标覆盖该标签之前保存的位置；异步内容到达后再次校正。新增回归覆盖往返切换。
- `pnpm --filter @teamcross/web check`、9 个文件共 120 项 Web 测试、`pnpm --filter @teamcross/web build` 通过，已更新 `internal/webassets/dist`。构建仍有主 bundle 超过 500 kB 的既有提示，本次未增加依赖。
- `go test ./...`、`go vet ./...` 及验证契约中的七个 Go 包 race test 通过。

## 真实浏览器结果

- 通过本机 Vite 预览连接现有 Core 的只读 API，在反馈使用的协作详情复核；没有调用模型、修改协作数据或重启用户 Core。
- 在 `968×1031` CSS 视口，四个上下文类型完整显示且无横向溢出。标签文字与目录、专注按钮的文字垂直位置相差约 1 CSS px；吸顶栏下缘至第一轮标题为 8 CSS px。
- 使用指针连续切换“代码改动 → 最近对话 → 代码改动 → 查看文件 → 技术信息 → 最近对话”，整页 `scrollY` 始终为 182，阅读栏标题顶边始终为 325.04 CSS px。
- 浅色和深色各在 `1440×900`、`1024×900`、`768×900` CSS 视口查看截图并读取 DOM 尺寸，共六个视图；页面横向溢出与标签横向溢出均为 0，标题下缘至第一轮标题均为 8 CSS px。
- 用户追加指出专注阅读工具栏中批注提示图标、目录图标和退出按钮高度不齐。重启 Vite 以清除旧 CSS 预览缓存后，在 `968×1030` CSS 视口的专注阅读状态复核截图；三个控件中心线分别为 471.375、471.365、471.370 CSS px，最大差异约 0.01 CSS px。此追加修改后再次通过 Web check、9 个文件共 120 项测试及 build，并更新内嵌资源；Go 代码未变。

## 详情页追加反馈

- 仅在具体协作详情路由隐藏全站顶部标语；将返回“协作空间”的链接放入标题上方的来源行。
- 将批注卡片的“加入选择 / 回复 / 复制提示”改成同一行的轻量操作区；回复使用浅蓝底强调，三项操作都有图标，选择和复制保持低调。已选择、收起回复和继续回复使用短文案，辅助名称与悬停提示保留完整含义。`968×1029` CSS 视口下操作区宽约 205.59 CSS px，默认三项按钮分别宽约 68、57、69 CSS px，均高 34 CSS px，顶边相同，页面没有横向溢出。
- 宽屏阅读栏的吸顶偏移由 12px 改为 0；普通阅读滚动到 `scrollY=1372.67`、专注阅读滚动到 `scrollY=1384` 时，阅读栏标题顶边均为 0，截图未见上方正文透出。窄于 600px 的移动端仍为顶部导航保留 61px。
- 追加修改后再次通过 Web check、9 个文件共 120 项测试和 build，`internal/webassets/dist` 已更新。Vite 预览连接现有 Core；没有修改协作数据。

## 批注与多份材料追加反馈

- 批注没有材料引用时不再渲染空的引用容器；批注意见到回复列表的间距由约 26 CSS px 收至 10 CSS px，回复计数到第一条回复作者的间距由约 18.8 CSS px 收至 12.8 CSS px。
- 展开材料不再改变卡片外框颜色。浏览器中展开前后卡片边框均为 `rgb(228, 232, 239)`，且页面没有横向溢出。
- 多份材料展开时，吸顶栏显示当前序号、固定版本与材料选择器，并保留收起入口；切换未读材料定位目标卡片，已读版本恢复位置，收起回到该卡片。键盘焦点跟随新选择器或收起后的卡片按钮。`968×1030` CSS 视口下，吸顶栏在 `scrollY≈482` 时顶边为 0，切换和收起操作在浏览器内均可完成。
- Vite 预览连接的现有 Core 不接受从开发源站发起的材料正文读取请求，因此浏览器复核覆盖工具栏和切换状态，长正文的实际读取没有在该预览中完成；Web 组件测试通过模拟公开正文覆盖切换及收起流程。

## 材料卡片操作与本地安装包

- 将发起者可用的“发布新版本 / 撤回”合成一组，放在卡片操作行右侧；展开阅读时沿用右对齐，小阅读栏空间不足则整组换到下一行。`966×1030` CSS 视口的真实浏览器中，收起和展开状态的作者操作组右缘均与操作区右缘一致，页面无横向溢出。
- 此追加修改后，Web 类型检查、9 个文件共 120 项测试和生产构建通过；构建已更新 `internal/webassets/dist`，`git diff --check` 通过。
- 使用 `make release VERSION=0.2.2-dev.material-actions-20260923` 构建独立的本地 arm64 安装包，保留其他版本产物。受限沙箱中的 `iconutil` 返回 `Invalid Iconset`，同一标准构建命令在主机环境成功；未替换图标来源。
- `make verify-release VERSION=0.2.2-dev.material-actions-20260923` 在主机环境通过：SHA-256、DMG 挂载与安装拷贝、App 图标、ad-hoc 签名完整性、CLI/App 版本一致性、隔离 App 生命周期和兼容 Core 复用均通过。沙箱内的 `hdiutil attach` 返回“设备未配置”，因此安装验证使用主机环境完成。
- 本地 DMG：`dist/release/0.2.2-dev.material-actions-20260923/Team-Cross-0.2.2-dev.material-actions-20260923-arm64.dmg`；SHA-256 为 `83299706a37dec9cd2e4c06604319a0ef0a3afe91c93737825ae3d9ccea474e2`。清单记录基线提交 `469f570ee1fb81b67ad2db1c40fff47e434e73d7`、`dirty: true`、macOS 14.0 最低版本，未使用 Developer ID 签名或 Apple 公证；这不是公开发行或真实 Codex/LAN 验证。

## 收尾与边界

首次验证后已关闭临时 Vite 与浏览器标签并恢复浏览器临时视口。用户随后要求保留预览，本次重新启动的 Vite 和预览标签继续开放；用户原有 Core 保留。此次前端验证不涵盖原生客户端、真实模型、两台 Mac 或 Tailcat 网络。
