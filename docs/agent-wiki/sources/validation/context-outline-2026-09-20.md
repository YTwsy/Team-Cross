# 2026-09-20 协作对话目录定位

范围：基线 `b92afc8` 加本次目录定位修复。环境为 macOS arm64、Go `1.27.1`、Node `24.18.0`、pnpm `11.19.0`。这是源码与本地构建验证，未更新已安装 App 或发布渠道。

## 问题与修复

历史页目录包含六轮，但正文预算只容纳第一轮时，旧目录点击仅查找已渲染元素，点击第六轮没有效果。

现在已加载轮次直接滚动，未加载轮次通过既有 `pageCursor + turnId` 读取接口加载后定位。继续分页、返回最近对话与批注定位清除轮次参数；读取失败可重试，切换标签后忽略迟到响应，加载期间保留专注阅读选择。窄屏滚动为固定顶栏保留 85 CSS 像素。

## 工程检查

- `go test ./...`、`go vet ./...` 通过。
- 验证契约列出的七个包的 `go test -race` 全部通过。
- Web `check`、`test`、`build` 通过，共 86 项测试；新增三项导航回归覆盖加载后滚动、已加载直接跳转、分页与返回、失败重试、专注状态和迟到响应。
- 已更新 `internal/webassets/dist` 并完成 `go build -o bin/teamcross ./cmd/teamcross`。
- `git diff --check` 与本次文档相对链接检查通过。

## 已完成的页面检查与边界

在用户要求停止浏览器验证之前，使用源码预览连接现有本机 Core，复现旧页面无跳转，并确认修复后第六轮正文可定位。完成 556×772、1024×900、1440×900 浅色和 768×900 深色截图复核；没有页面水平溢出。窄屏目标标题约在视口 85 像素处，位于 61 像素高的固定顶栏下方；宽屏跨轮加载后专注阅读保持开启。

测试结束恢复主题与视口，关闭本次测试页面；现有 Core、开发服务和用户页面保留。后续按用户要求不再追加浏览器验证。本轮未调用真实模型、操作原生客户端或执行跨设备网络测试。

相关：[上下文阅读](../product-flows.md#上下文阅读) · [导航回归](../../../../packages/web/src/test/context-refresh.test.tsx) · [验证门槛](test-gates.md)。
