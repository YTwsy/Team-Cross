# 2026-09-23 资源库浏览顺序稳定性

基线为 `930ea1b` 加本次工作区改动，环境为 macOS 14.8.5、Go 1.27.1、Node 24.18.0。行为约定见 [资源库决策](../decisions/resource-library.md)。

## 问题与修复

点击阅读会保存最近打开时间，Core 返回按最近使用重排的资源。此前前端直接替换列表，导致点击的条目及其分组立即置顶，定时刷新也会再次应用该排序。

[资源库状态](../../../../packages/web/src/library.tsx) 现在按当前分组和条目顺序合并新的资源快照，只保留显示次序，始终使用新的内容、选择、收藏与访问状态。新资源追加，消失的资源立即移除；某分组首项消失时，其余条目仍保留该分组的位置。页面顶部手动刷新采用服务端排序，访问记录和接口契约保持不变。

## 工程验证

- [资源库交互测试](../../../../packages/web/src/test/library.test.tsx) 的模拟响应现已覆盖阅读后服务端重排。新增两项用例先在旧实现上复现失败，再验证组内及跨组位置稳定、勾选与收藏、后台更新、显式刷新，以及资源增删和访问变化。
- Web 类型检查、全部 112 项测试和 production build 通过，已更新 `internal/webassets/dist`。
- `go test ./...`、`go vet ./...`、验证契约所列七个包的 race test 以及本地 CLI 构建通过。Vite 仍提示主 bundle 超过 500 kB，本次没有改动拆包策略。
- 本次文档相对文件链接与 `git diff --check` 通过。

## 真实浏览器

使用 [独立资源库 fixture](../../../../internal/collab/library_browser_test.go) 加载本次 production build，包含三个分组、五条资源；Core、HTTP 和存储真实，来源历史为合成内容。

1. 点击末组末项“连接池等待与重试策略”，读取预览后，五个条目和三个分组的顺序均保持不变。
2. 勾选并收藏该项，状态正确更新，条目及分组保持原位。
3. 浏览器网络记录确认连续五次 `/api/library` 后台请求返回 200；列表顺序仍保持不变。
4. 点击顶部“刷新”后，该项及所属分组按最近使用排到前面，阅读预览与勾选状态保留。
5. 在实际 CSS 视口 `1440×900`、`1024×900`、`768×900` 的浅色与深色下检查，无页面横向溢出或目录越界，当前阅读与条目顺序稳定。实际查看了宽屏阅读与窄屏深色截图，控制台无 error。

## 收尾与边界

已关闭本次测试页面并恢复视口、主题模拟，通过 fixture 的 `finish` 信号退出测试 Core，用例最终通过。用户正在使用的服务与页面未重启；新构建需要在服务重新加载后生效。本次未调用真实模型、操作原生客户端或验证跨设备网络。
